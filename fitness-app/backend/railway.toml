[build]
builder = "nixpacks"

[deploy]
startCommand = "alembic upgrade head || echo 'Migration failed, continuing anyway'; uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
